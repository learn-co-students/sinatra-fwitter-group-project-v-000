<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Tweets of <%= @user.username %></title>
  </head>
  <body>
    <h2>Tweets of <%= @user.username %></h2>

    <h3>Welcome,</h3>

    <% @user.tweets.each do |tweet_obj| %>
      <div>
        <p>
          <%= tweet_obj.content %>
        </p>
      </div><br>
    <% end %>

  </body>
</html>
