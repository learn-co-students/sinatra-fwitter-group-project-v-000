<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%=@current_user.username%></title>
  </head>
  <body>
    <h1><%=@current_user.username%></h1>
    <% @current_user.tweets.each do |tweet| %>
    <ul>
      <li><%=tweet.content%></li>
    </ul>
    <%end%>
  </body>
</html>
