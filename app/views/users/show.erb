 #get "/users/#{user.slug}"
 
 
 
 <!DOCTYPE html> 
<html>
  <head>
    <meta charset="UTF-8" />
    <title> User Tweet Index</title>
  </head>
<body>
  <h1> <%= @user.username %> 's Tweets</h1>
  
  <% @user.tweets.each do |tweet|
  
  <%= @tweet.content %>
   
   <% end %>
   
  <a href='/logout'> Logout Here </a>

  </body>
</html> 