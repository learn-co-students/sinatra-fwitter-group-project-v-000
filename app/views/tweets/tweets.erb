<h1>Welcome, <%= @user.username %>! </h1>

  <ul>
    <% Tweet.all.order('id DESC').each do |tweet| %>
    <li><a href="/tweets/<%= tweet.id %>"><%= tweet.user.username%><br><%= tweet.content %></a></li>
    <% end %>
  </ul>
