<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Welcome, <%= current_user.username %> to your Fwitter Page!</title>
  </head>
  <body>
    <h2>Welcome, <%= current_user.username %> to your Fwitter Page!</h2>
    <p>Create a <a href="/tweets/new">New</a> Tweet! | <a href="/logout">Log Out</a></p>
    <h2>Tweets</h2>
    <ul>
      <% Tweet.all.each do |tweet| %>
        <li> <a href="/tweets/<%= tweet.id %>"><%= tweet.content %></a> - <a href="/users/<%= tweet.user.slug %>"><%= tweet.user.username %></a></li>
      <% end %>
    </ul>
  </body>
</html>
