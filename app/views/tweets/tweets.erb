
<% if Helpers.is_logged_in?(session) %>
<% @user = User.find_by( username: session[:username] )%>
  "Welcome, <%= @user.username %> look at this tweet"
  <% @user.tweets.each do |tweet|%>
    <%= tweet.content %>
  <% end %>
<% else %>
  <% redirect to '/login' %>
<% end %>
