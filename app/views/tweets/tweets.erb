<h1> Welcome, <%= current_user.username %></h1>
<h2> Tweets </h2>

<% @tweets.each do |tweet| %>
  <div>
    <%= "#{tweet.user.username.upcase} wrote \n '#{tweet.content}'" %><br><br>
  </div>
<% end %>

<form method="get" action="/tweets/new">
  <input type="submit" value="Create new Tweet?">
</form>
