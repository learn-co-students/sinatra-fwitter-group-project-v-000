<div class="container center">
  <h1>Welcome, <%= current_user.username %>!</h1>
  <hr>
  <h2>Tweets</h2>

  <% @tweets.each do |tweet| %>
    <div>
      <%= tweet.content %>
    </div>
  <% end %>
</div>
