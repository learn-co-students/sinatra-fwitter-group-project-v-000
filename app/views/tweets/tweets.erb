<h2><em>Welcome, <%= User.find(session[:user_id]).username.capitalize %></em></h2><br>

<% if @tweets.nil? %>
    <p class="lead">There are no tweets</p>
  <% else %>
    <ul>
      <% @tweets.each do |tweet| %>
      <li class="lead"><%= tweet.content %></li>
      <% end %>
    </ul>
<% end %>
