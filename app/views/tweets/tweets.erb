<h1> All the Tweets...Need to add Welcome, current_user.name and add each Tweet's username </h1>

<% @tweets.each do |tweet| %>
  <li> <a href="/tweets/<%= tweet.id %>"> <%= tweet.content %> </a></li>
<% end %>
