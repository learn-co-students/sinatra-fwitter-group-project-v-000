<h3>Welcome, <strong><%= user.username %></strong></h3>

<% @tweets.each do |t| %>
    <div class="tweetContainer">
        <span><%= t.content %></span><br>
        <span>tweeted by: <%= t.user.username%></span>
    </div>
<% end %>