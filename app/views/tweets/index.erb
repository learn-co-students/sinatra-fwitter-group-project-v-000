<h1>Welcome, <%= @user.username %> </h1>

<% @tweets.each do |tweet| %>
  <ul>
    <li><%= tweet.content %> --<%= User.find_by_id(tweet.user_id).username %></li>
  </ul>
<% end %><br></br>


<a href="/logout">Log Out</a>  &nbsp;  &nbsp;
<a href="/tweets/new">Send a Tweet</a>
