<h1>Welcome, <%= @user.username%>!</h1>

<h2> Tweets </h2>

<%if !Tweet.all.empty? %>
  <% Tweet.all.each do |tweet|%>
    <a href="/tweets/<%=tweet.id%>"><%= tweet.content %></a>
    <br></br>
  <% end %>
<% else %>
  There are no tweets!
<% end %>
