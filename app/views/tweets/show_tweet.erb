<% if !logged_in? %>
  <% redirect '/login' %>
<% end %>
<% if logged_in? %>
	Your new tweet: <%= @tweet.content %>
	<!-- <a href="/delete">Delete Tweet</a> -->
	<a href="/tweets/<%= @tweet.id %>/edit" >Edit Tweet</a>
	<form action="/tweets/<%=@tweet.id%>/delete" method="POST">
		<input type="submit" value="Delete Tweet">
	</form>
<% end %>