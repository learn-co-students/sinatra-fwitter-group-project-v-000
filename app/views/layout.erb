<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Fwitter</title>
  </head>
  <body>
    <h1>Welcome to Fwitter</h1>
    <div>
      <% if logged_in? %>
        <a href="/tweets/new">Create Tweet</a>
        <a href="/logout">Log Out</a>
        <p> Welcome, <%=current_user.username %></a>

      <% else %>
        <a href="/signup">Sign Up</a>
        <a href="/login">Sign in</a>
      <% end %>
    <div>

    <%= yield %>

  </body>
</html>
