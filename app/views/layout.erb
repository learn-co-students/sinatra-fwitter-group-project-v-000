<!doctype html>
<html>
  <head>
    <title>Fwitter</title>
  </head>
  <body>

    <div class="container">
      <% if session[:user_id]%>
        <h1>Welcome, <%= User.find(session[:user_id]) %></h1>
      <% else%>
        <h1>Welcome to Fwitter!</h1>
      <% end %>
      <%= yield %>
    </div>

  </body>
</html>
