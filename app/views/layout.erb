<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <header>
      <h2>
        <mark><b><a href="/">Fwitter</a></b></mark>
      </h2>
      <%if logged_in?%>
        <p>Logged in as: <b><%=current_user.username%></b> <a href="/logout"><small>Log Out</small></a></p>
      <%else%>
        <p>
          <small><a href="/signup">Sign Up</a></small>
          <small><i>already user?</i><a href="/login">Log In</a></small>
        </p>
      <%end%>
    </header>
    <%= yield %>
  </body>
</html>
